[project]
name = "airflow-superglm-builder"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
  "azure-identity",
  "joblib",
  "numpy",
  "openpyxl",
  "pandas",
  "pydantic>=2.13,<3",
  "pyodbc",
  "python-dotenv",
  "scikit-learn",
  "sqlalchemy",
  "superglm[editor] @ git+https://github.com/StrudelDoodleS/superglm.git@b91fbef5f1ef15aadfa0372963fed3864607d816",
]

[project.optional-dependencies]
mlflow = ["mlflow"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
target-version = "py314"
line-length = 100

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "sqlfluff>=4.2.1",
]
